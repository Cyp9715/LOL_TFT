<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lol_tft">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
    <!--android:theme="@style/AppTheme"-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Percentage_Activity" android:label="Champion 액티비티"/>
        <activity android:name=".Champion_Activity" android:label="Champion 액티비티"/>
        <activity android:name=".Synergy_Activity" android:label="Champion 액티비티"/>
        <activity android:name=".Synergy_Combine_Activity" android:label="Champion 액티비티"/>
        <activity android:name=".Path_Note_Activity" android:label = "Path_Note 액티비티"/>

    </application>

</manifest>